import React from "react";
import Header from "./Header";

export default {
  title: "Components/Header", // Storybook category
  component: Header, // Component to render
};

export const Default = () => <Header />;
